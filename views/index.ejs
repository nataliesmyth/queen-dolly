<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Express events</title>
</head>
<body>

  <header>
    <nav>
      <a href="/">Home</a>
      <a href="/events">events</a>
      <a href="/events/new">Add event</a>
    </nav>
  </header>

  <main>
    <h1>Events Index Page</h1>

    <% if (events.length) { %>
      <% for (let i = 0; i < events.length; i++) { %>

        <a href="/events/<%= events[i]._id %>">
          <h4><%= events[i].name %></h4>
        </a>
        
        <!-- Delete Button -->
        <form style="display: inline-block;" method="POST" action="/events/<%= events[i]._id %>?_method=DELETE">
          <button type="submit">Delete</button>
        </form>
  
        <a href="/events/<%= events[i]._id %>/edit">
          <button>Edit</button>
        </a>
  
  
        <hr/>
      <% } %>
    <% } else { %>
      <h4>Sorry, doesn't look like we have any event</h4>
    <% } %>

    
    

  </main>

</body>
</html>